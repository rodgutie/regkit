# Immutable Audit Log — Specification & Coverage Report

**Project:** test-healthtech-fintech-app  
**Generated:** 2026-06-25 by RegKit AGENT-5  
**Regulatory basis:** EU AI Act Article 12 (automatic logging, reconstructable operation); HIPAA § 164.312(b) (audit controls); 10-year retention for medical AI  

> **Disclaimer:** This document was auto-generated by RegKit from a static + reasoning scan of source code. It is a compliance-intelligence draft, not legal advice, and not a substitute for review by qualified legal counsel. Sections marked _[HUMAN COMPLETION REQUIRED]_ contain determinations RegKit cannot make from code alone.

## 1. Why this log is required

EU AI Act Article 12 requires high-risk AI systems to automatically record events ("logs") over the system's lifetime, with sufficient detail to identify situations that may result in risk or substantial modification, and to enable post-hoc reconstruction of how a decision was reached. HIPAA separately requires audit controls recording access to ePHI. Retention is up to 10 years for medical AI.

## 2. Required log event schema _(auto-generated)_

Each AI decision event must capture, at minimum:

| Field | Purpose |
|---|---|
| timestamp (UTC) | when the decision occurred |
| system_id + version | which model/version produced it |
| input_reference | hashed/redacted reference to the inputs used (not raw PII) |
| output / decision | the AI's output |
| human_review_status | whether a human reviewed/overrode (links to AGENT-4 humanReviewGate) |
| jurisdiction | which legal regime applied |
| immutable_hash | tamper-evidence (prev-hash chain) |

## 3. Logging coverage by AI touchpoint _(auto-generated)_

| AI Touchpoint | Logging present? | Action |
|---|---|---|
| `evaluateLoanApplication()` | ⚠️ not detected | Inject `regkit.trace()` via AGENT-4 `--fix` |
| `assessPatientRisk()` | ⚠️ not detected | Inject `regkit.trace()` via AGENT-4 `--fix` |

## 4. How RegKit activates this log

AGENT-4 injects `regkit.trace()` calls as part of the auto-fix for Article 12 findings. The developer accepts the fix once; logging then runs automatically on every subsequent decision, writing immutable, hash-chained events. This document regenerates from those logs to show coverage over time.

## 5. Retention & immutability attestation _[HUMAN COMPLETION REQUIRED]_

- [ ] Retention period configured (10 years for medical AI; per-regime otherwise)
- [ ] Logs stored in append-only / WORM storage
- [ ] Tamper-evidence (hash chain) verified
- [ ] Access to logs itself audited

